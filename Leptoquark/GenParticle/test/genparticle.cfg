process Demo =  {
  
source = PoolSource 
{ 
     # replace 'myfile.root' with the source file you want to use
     untracked vstring fileNames = {	
				     // add info on how to access data from castor at cern and dcache at fnal
				     //"file:myfile.root"	
				     "file:/u1/santanas/GMSB_V1_SIM146_REC164_236_1_FEVT-100evt.root"
				   } 
}

untracked PSet maxEvents={ untracked int32 input = 10 }

#keep the logging output to a nice level
service = MessageLogger {
	//untracked vstring categories = {"Demo"}
	}

module demo = GenParticle { 
}

service = TFileService {
     string fileName = "GenParticle.root"
}

path p = {demo} 

}
